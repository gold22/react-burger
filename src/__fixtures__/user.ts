import { TApiUserInfo } from '../services/types/api';

// eslint-disable-next-line import/prefer-default-export
export const userInfo: TApiUserInfo = {
    name: 'Test',
    email: 'test@example.tld',
};
